{
  "name": "blitz",
  "version": "1.0.0",
  "workspaces": [
    "ThriftTsConfig"
  ],
  "scripts": {
    "config": "npm run -w ThriftTsConfig config -- --dir src/config",
    "generate-thrift": "npm run -w ThriftTsConfig generate-thrift --"
  }
}
